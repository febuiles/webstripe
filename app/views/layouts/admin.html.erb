<!DOCTYPE html>
<html>
  <head>
    <title>webstripe</title>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag    "admin", :media => "all" %>
    <%= javascript_include_tag "admin" %>
    <link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,600,700,400italic,600italic' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Anton' rel='stylesheet' type='text/css'>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="wrap">
      <div class="admin-sidebar">
        <h1 class="admin-header">webstripe</h1>
        <h2 class="projects"><i class="icon-tasks"></i> Stripes</h2>
        <ul class="nav nav-pills nav-stacked">
          <% @stripes.each do |stripe| %>
            <li>
              <%= link_to stripe_path(stripe) do %>
                <%= stripe.title%> <i class="icon-circle-arrow-right"></i></a></li>
              <% end %>
            </li>
          <% end %>
          <li><%= link_to "Create New Stripe", new_stripe_path, :class => "important" %></li>
          <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
        </ul>
      </div>
      <div class="admin-content">
        <%= render "shared/notices" %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>

