<!DOCTYPE html>
<html lang="en">
  <head>
	  <meta charset="utf-8" />
	  <title>Webstripe</title>
		<%= stylesheet_link_tag "static"%>
    <%= javascript_include_tag "static" %>
		<link href="/assets/favicon.ico" rel="icon">
    <link href="http://fonts.googleapis.com/css?family=Vollkorn:400italic,400,700" rel="stylesheet" type="text/css">
    <link href='http://fonts.googleapis.com/css?family=Share:400,700' rel='stylesheet' type='text/css'>
	  <!--[if lt IE 9]>
	  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
	  <![endif]-->
		<%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= render "shared/ga" %>
	</head>
  <body>
		<!--start header-->
  	<header>
		<%= link_to "", admin_path, :id => "logo"%>
			<nav id="top-left">
      	<ul>
      		<li><%= link_to "Help", help_path %></li>
      	</ul>
      </nav>
			<nav id="top-right">
      	<ul>
					<li><%= link_to "Login", new_user_session_path %></li>
      	</ul>
      </nav>
		</header>
		<!--end header-->
		<!--container-->
		<div class="container">
		  <div class="page-title">
		    <div class="title-flag">Sign in</div>
		  </div>
      <div class="data-container">
          
        <div class="quote">Just four details please --</div>

				<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => { :method => :put } do |f| %>
				<%= devise_error_messages! %>

				<%= f.hidden_field :invitation_token %>
				<p><%= f.label "Name:" %>
				<%= f.text_field :name %></p>
				
				<p><%= f.label "Email:" %>
				<%= f.password_field :email %></p>
				
				<p><%= f.label "Username:" %>
				<%= f.text_field :username %></p>

				<p><%= f.label "Password:" %>
				<%= f.password_field :password %></p>

				<p><%= f.submit "Submit" %></p>
				<% end %>
				
				<p class="terms-text">- by clicking you accept our <%= link_to "terms & conditions", terms_path , :target => "_blank" %></p></br>
      </div>
    </div>
		<!--end-container-->
		<!--footer-->
				<footer>
					<p id="footer-data-static">
						Webstripe | All rights reserved © 2013</br>
		        <%= link_to "info@webstripe.co", "mailto:info@webstripe.co" %></br>
		        <%= link_to "@WebstripeCo", "http://twitter.com/webstripeco", :target => "_blank" %>
		      </p></br>
				</footer>
		<!--end-footer-->
  </body>
</html>